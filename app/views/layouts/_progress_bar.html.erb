<div class="progress-list-flex">
  <% guide.exercises.each do |e| %>
    <a href="<%= organization_exercise_path(Organization.current, e)%>"  title="<%= e.navigable_name %>"  class="<%= class_for_progress_list_item(e, e == actual)%>">
    </a>
  <% end %>
</div>
